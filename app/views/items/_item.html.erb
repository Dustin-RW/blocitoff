<% @user.items.each do |item| %>
  <ul>
    <p id="item-<%= item.id %>"><%= item.name %> | <%= link_to "", [@user, item], method: :delete, class: 'glyphicon glyphicon-ok', remote: true %></p>
  </ul>
<% end %>
